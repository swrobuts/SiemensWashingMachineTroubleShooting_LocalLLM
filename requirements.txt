# --- Web / API ---
flask>=3.0
flask-cors>=4.0

# --- LlamaIndex RAG (lokal) ---
llama-index-core>=0.11
llama-index-llms-openai-like>=0.2     # LM Studio: erlaubt echte lokale Modellnamen
llama-index-embeddings-huggingface>=0.3
sentence-transformers>=3.0            # zieht torch; lokales Embedding (e5/bge)

# --- Dokument-Extraktion (parser.py) ---
docling>=2.0

# --- PageIndex (vectorless, reasoning-based Retrieval; Paket vendored in pageindex/) ---
litellm>=1.84          # LLM-Abstraktion, lm_studio-Provider → lokal
pymupdf>=1.26
PyPDF2>=3.0
pyyaml>=6.0
python-dotenv>=1.0

# --- Tests ---
pytest>=8.0
